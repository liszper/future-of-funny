'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _resolution = require('../resolution');

var _resolution2 = _interopRequireDefault(_resolution);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

exports.default = {

  MAX: 59,

  resolution: _resolution2.default.Minute,

  datepart: function datepart(_ref) {
    var date = _ref.date;
    return date.getMinutes();
  },

  every: function every(_ref2) {
    var date = _ref2.date;
    var interval = _ref2.interval;

    var seconds = date.getMinutes();

    var result = new Date(date);
    result.setMinutes(seconds + interval);

    return result;
  },

  on: function on(_ref3) {
    var date = _ref3.date;
    var interval = _ref3.interval;

    var result = new Date(date);
    result.setMinutes(interval);

    return result;
  }
};